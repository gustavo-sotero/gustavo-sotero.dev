// Stub for `server-only` — used in Vitest test environment.
// In production Next.js builds, the real package throws if imported from a Client Component.
// Here we export nothing so tests can import server modules without the guard.
export {};
